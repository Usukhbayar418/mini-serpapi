#!/bin/bash -e

# Ensure database is created and migrated before any command runs.
# db:prepare is idempotent: it creates the DB if missing and runs
# any pending migrations.
./bin/rails db:prepare

exec "${@}"